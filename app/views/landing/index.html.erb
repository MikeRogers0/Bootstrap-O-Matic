<div class="container">
  <div class="row">
    <div class="col-12 col-md-6 offset-md-3">
      <div class="mt-3 mb-4 text-center">
        <p class="lead">Have you ever picked up a bootstrap project &amp; just wished there was a living styleguide for it?</p>

        <p class="lead">Styleguide-o-matic fixes that.<br />Just pop your CSS into the form below and let it do its magic.<p>
          </div>

          <div class="mt-4 mb-4">
            <%= render 'css_parsers/form', resource: ParseCSSForm.new %>
          </div>

          <h2 class="text-center h4">Or</h2>

          <div class="mt-4 mb-4">
            <%= simple_form_for :random_css_form, url: css_random_path do |f| %>
              <div class="form-actions text-center">
                <%= f.submit class: 'btn btn-secondary' %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
